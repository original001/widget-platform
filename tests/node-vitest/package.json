{
    "type": "module",
    "private": true,
    "sideEffects": false,
    "devDependencies": {
        "@skbkontur/loader-builder": "*",
        "@skbkontur/operation-result": "*",
        "ts-expect": "^1.3.0",
        "typescript": "^5.3.3",
        "vitest": "^1.6.0"
    },
    "scripts": {
        "test": "vitest run --reporter=default --reporter=junit --outputFile=../../.artifacts/e2e-node/junit.xml"
    }
}
