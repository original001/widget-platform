// Открой этот файл сразу после открытия IDE, чтобы инициализировать в IDE корневой tsconfig.json, который включает все ts-файлы, которые не включены в иные tsconfig
// Необходимо для VS Code и WebStorm.
// Без открытия этого файла, перечисленные IDE не запустят процесс, использующий корневой tsconfig.json. А без этого в файлах, которые не включены в ближайший tsconfig.json будут ошибки.
